---
title: 专栏写作规范
date: 2026-09-12
tags: [writing, meta]
column:
order:
viewable: true
---

# 专栏写作规范

本仓库所有技术专栏（`claude-code`、`claude-agent-sdk`、`linux 命令入门`、`golang_mcp`…）统一按本文组织。新开专栏照做即可。

样板：`golang_mcp/`。规范里的每条规则都在那儿有落地实例。

---

## 一、四条核心原则

**1. 目录结构只表达「阅读顺序」。**

不要用用途给目录命名。`教程/`、`指南/`、`进阶/` 这类目录名会把"必经步骤"暗示成"可选备查"，读者看到的是菜单而不是路径。

**2. 文体是「写法」，不是「目录」。**

一篇文档是教程、指南、解释还是参考，体现在**它内部的骨架**上（任务型开头写"什么时候需要"，教程型开头写"先看效果"），不需要分目录。

**3. 难度是「标签」，不是「目录」。**

可选内容留在主线里、按连续编号排下去，只在标题和 README 里标「可选」。理由：把可选内容挪出主线，受伤的是**真的想看它的人**——他得离开这条路，回来还得重新找位置。

> 「可跳过」和「在主线外」是两回事。前者不破坏链条，后者破坏。

**4. 主线是「需求」，不是「系统的功能清单」。**

专栏的组织线应该是「用户会依次遇到的需求」，不是「这个系统有哪些功能」。每篇从一个真实场景出发（我想……，所以要看这篇），只引入满足它所需的**最小机制**；框架的其余部分，等下一个需求逼到它时再出现。

判断一篇该不该写，问「它把一个真实需求往前推了一步吗」，而不是「这个特性还没讲过」。代价要认：这样排必然有功能讲不到——那是对的。覆盖是副产品，不是目标；查缺补漏交给参考文档。

---

## 二、三类内容的处置

| 内容 | 放哪 | 判断标准 |
| --- | --- | --- |
| **主线** | 专栏根目录，连续编号，有序 | 它是「终点能力」的必经步骤吗？是 → 进主线 |
| **可选** | 同一序列的尾部，编号不断，标「可选」 | 不做它也能达到终点，但做了更好 |
| **参考** | `参考/` 子目录，不编号 | 它是**按定义就不该顺序读**的东西吗？字典、字段表、错误码、对照表 |

**只有参考需要物理分离**，因为查阅型内容按定义不需要顺序。**参考卡片不编号、也不写 `order`**——它不在阅读序列里，就不该占文章号。其余一切对初学者都是顺序读的。

反例：把 `端点续传`（可选）放进 `进阶/` 目录——正确做法是留在编号序列的 10 号位，标题标「可选」。

有些专栏**不需要** `参考/` 目录。例如 `linux 命令入门` 的主体本来就是命令速查，硬加一个参考目录是形式主义。**有查阅型内容才分，没有就不分。**

---

## 三、README 的固定结构

每栏必须有 README，且按这个顺序写：

```markdown
## <专栏名>

> 官方文档：<链接>

<一句话：这个专栏教什么，用什么>

### 读完能做到什么
**能**：<具体能力，可验证>
**不能**：<明确划出边界，这是取舍的依据>

### 前置假设
- <读者需要已经会什么>
- <不需要预先了解什么>

### 一条主线
<如果多篇共用同一个案例，说明是哪个；否则说明「每篇各用最适合的例子」>

**下面按顺序读就行**——每篇开头写了前置是哪篇，结尾有下一篇的链接。

1. [篇名](文件.md)——一句话
2. ...
9. [篇名](文件.md)——**可选**，什么情况下再看

### 只想找某一件事
| 我想…… | 看这篇 |

### 参考卡片
<不是读物，是查到才用的字典>
```

**几个要点：**

- **「不能」比「能」更重要。** 它是取舍的判据：任何内容都能靠它判定去留。没有这句，专栏必然越长越杂。
- **主线只能有一个有序列表。** 分组成多个列表就等于把路径变成了菜单。
- 任务索引表是**附属**的，给已经会了、不想顺序读的人用，不能取代主线列表。
- 可选条目在主线列表里标注，不要另起一节「进阶」。

---

## 四、单篇的固定结构

### 开头三行（必须有）

```markdown
> **读完这篇你能**：<可验证的结果>。
>
> **前置**：<哪一篇>。约 <N> 分钟。
```

### 结尾（必须有）

```markdown
---

**下一篇**：[篇名](文件.md)——<一句话>。
```

最后一篇写终点标记 + 回头补读的入口：

```markdown
**主线到此结束。** 中间跳过的可以回头补：[X](X.md)、[Y](Y.md)。
```

### 正文

教程型：

```markdown
# 先看效果          ← 30 秒能验证的目标，先给结果
# 完整代码 / 改哪里   ← 只给 delta，不重贴整个文件
# 代码解释          ← 概念，一节一个
```

任务型（指南型）：

```markdown
# 什么时候需要       ← 先说清适用场景，不适用的人可以走
# 改哪里            ← 最小改动
# 完整代码
# 注意事项
```

### 分支要给「回归点」

有跳过路径时，在**上一篇的结尾**和当篇开头都说清楚跳过去哪儿：

```markdown
**下一篇**：[挂到 gin 路由](04-挂到gin路由.md)。

不用 gin 的话跳过它，直接去[提供 Resources 知识库](06-提供Resources知识库.md)。
```

---

## 五、写作硬规则

1. **一篇只回答一个问题。** 标题就是那个问题或任务。「琐碎内容」「概念理解」这类标题说明这篇还没想清楚。
2. **代码只给 delta。** 同一个函数名在专栏里语义恒定，读者每次只需看「这一步改了什么」。完整文件只在第一篇给一次。
3. **字段罗列一律进表格，表格一律进参考。** 凡是你写了"完整说明见 pkg.go.dev"的地方，上面那张表就该整体搬走。
4. **故事只用于建立动机，进代码就停。** 讲"为什么要这个能力"可以两段，不要讲一个完整故事。
5. **例子跟着主题走，不是跟着专栏走。** 每篇挑最能说明该概念的例子——讲服务就举一个真的需要可替换 provider 的能力，讲事件就举一个真的靠广播解耦的场景。只有连续几篇讲同一件事时才延续同一个案例，延续了就不要中途换。不要为了「全栏共用一个例子」把不合适的案例硬套到每一篇上。判断标准只有一个：这个例子是不是把该需求讲得最清楚，而不是「它跟上一篇是不是同一个」。
6. **数字/工具名跨篇一致。** 同一个工具名在不同篇里 schema 不能冲突。

---

## 六、文件与 frontmatter 约定

```yaml
---
title: <标题>            # 见名知意，不用「杂项」「其他」
date: <YYYY-MM-DD>
tags: [<标签>]
column: <专栏 id>        # 与 config.yml 的 columns[].id 一致；不属于任何专栏则留空
order: <整数>            # 决定阅读顺序，全专栏唯一、连续；参考卡片不写这个字段
viewable: true
---
```

**文件名一律带编号**：`NN-描述.md`，`NN` 与 frontmatter 的 `order` 取值一致（补零到两位）。同一个事实有两种写法，改了一处必须改另一处——`tool/check-columns.sh` 会校验两者一致，对不上直接报错。

- **不编号的只有两类：`README.md` 和 `参考/` 下的卡片。** `README.md` 是专栏首页而不是文章；而且 `config.yml` 的 `exclude` 是按 `README.md` 这个确切文件名把它排除在发布之外的，一旦改名就会被当成文章发出去。
- **参考卡片不编号、也不写 `order`。** 它按定义不在阅读序列里，所以既不占文章号，也不参与 `order` 唯一性和篇尾链路的校验（`tool/check-columns.sh` 会跳过 `参考/`）。文件名的顺序也不再承诺等于站点排列顺序——想固定参考卡片的展示顺序，就在 README 的「参考卡片」一节里列。
- 正文文章的编号只是让顺序在文件管理器里也看得见；真正的排序仍由 `order` 驱动（见本仓库 `sortspec.md`）。

> 代价换得明白：已发布的专栏改名，如果站点是按文件名生成 URL 的，旧链接会失效。本仓库当前的判断是文件名不进 URL（已发布文章里存在带空格的中文文件名），所以统一加编号没有额外风险；换站点或改构建配置时，这一条要重新评估。

**关于 `column`：** 留空是本仓库「不属于任何专栏」的惯例（见 `dsh/`、`tool/`）。新增专栏要在 `config.yml` 的 `columns:` 里注册，否则站点不会生成专栏落地页。

---

## 七、自检清单

改完一栏，先跑脚本再逐条过一遍：

```bash
tool/check-columns.sh              # 检查所有专栏
tool/check-columns.sh golang_mcp   # 只检查一栏
```

脚本会校验：文章间链接可解析、图片存在、`order` 唯一、无孤儿文件、篇尾链路完整。剩下的靠人看：

- [ ] README 有「能做什么 / 不能做什么」
- [ ] 主线是**一个**有序列表，编号连续无重复
- [ ] 可选条目在主线列表里，没有独立目录
- [ ] 每篇有开头三行（读完你能 / 前置）
- [ ] 跳过的分支给出了回归点
- [ ] 每个文件名是 `NN-描述.md`，且 `NN` 与 `order` 一致（`README.md` 与 `参考/` 除外）
- [ ] 没有「杂项 / 其他 / 琐碎」类标题
- [ ] 长文件按「一篇一问题」拆过（参考卡片除外）
- [ ] 引用示例代码时，示例里的路径用行内代码而不是链接（否则渲染成死链）

---

## 八、反面样板：golang_mcp 重构前后

**改之前**（15 个文件 / 1741 行）：

```
golang_mcp/
├── README.md          说"学习笔记"，但内容像教程
├── Server 端/         10 篇，52～314 行均匀分布
└── 参考/              4 篇（其中 2 篇是素材，1 篇是一次性测试数据）
```

问题：

- 没声明「能/不能」，所以什么都往里塞
- 案例换得没有理由：`hello_world` → 日志系统 → MySQL 结算行，讲的其实是同一件事（定义一个 MCP 工具），却每篇另起一个互不相关的案例，读者要重建 4 次上下文
- 同一个工具名 `query-logs` 在相邻两篇里 schema 冲突（单选 `log_level` vs 多选 `log_levels`）
- 错误码表、Transport 对照表这类参考内容埋在教程正文里
- `端点续传`（可选）排在阅读顺序第 4 位，夹在主线中间

**改之后**（19 个文件 / 1874 行，行数变多是对的——取舍的体现是**主线变薄**，不是总量变小）：

```
golang_mcp/
├── README.md                    终点承诺 / 前置 / 一条有序主线 / 任务索引 / 参考卡片
├── 01-第一个MCP.md … 10-SSE断点续传.md    单一编号序列，09、10 标「可选」
└── 参考/                        8 张卡片
```

主线教程从 10 篇 1479 行收敛到 3 篇 421 行；进阶两篇 115 行；参考 513 行。读者路径从"10 篇里自己挑"变成"01→10 一条链"。
